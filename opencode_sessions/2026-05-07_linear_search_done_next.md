# Конспект: 2026-05-07 — L1 завершён, планирование следующего шага

## Контекст
Ирина завершила L1 линейного поиска (C и C++ версии), скомпилировала и запустила из PowerShell — всё работает. Сессия: сориентироваться по дальнейшим шагам и составить план на день.

## Ключевые шаги диалога

### 1. Планирование
- Ирина запросила план на сегодня.
- Агент определил: репозиторий есть на GitHub (`Miagiri/practice_Cpp`), но локально remote не настроен, коммитов нет.
- План: (1) первый коммит с самопроверкой, (2) раздельные этапы сборки, (3) build.bat/build.ps1.

### 2. Первый коммит и push
- Настроен remote: `git remote add origin https://github.com/Miagiri/practice_Cpp.git`
- `clang-format` оказался не в PATH — запущен через полный путь `C:\Program Files\LLVM\bin\clang-format.exe -i`.
- clang-format убрал пустые строки с пробелами в обоих файлах.
- C-версия собралась чисто с `/W4 /analyze`.
- C++-версия дала warning C4530 — требовался `/EHsc`. После добавления — чисто.
- Удалён `my_test.cpp`.
- Настроен git config: `user.name "Irina Miagkova"`, `user.email "ravevas@yandex.ru"`.
- Коммит: 14 файлов, 2541 строка.
- Push завис — интерактивная авторизация GitHub не поддерживается инструментом. Ирина запушила сама в PowerShell.

### 3. Замечание про /std:c++latest для .c
- Ирина заметила, что флаг `/std:c++latest` не нужен для C-файла.
- Подтверждено: `cl.exe` определяет язык по расширению, C++-флаг для `.c` игнорируется.
- Правильные ключи для C: `cl /W4 /analyze /Zi /MD file.c`.
- Ирина пересобрала C-версию без лишнего флага.

### 4. Раздельные этапы сборки
- Ирина выполнила `cl /c` → `.obj` → `link` для обеих версий.
- Увидела разницу: компиляция (`/c`) даёт `.obj`, компоновка — `.exe`.

### 5. build.ps1
- Ирина хотела `.bat`, агент объяснил проблему с переменными окружения при переходе CMD→PowerShell.
- Предложен вариант А: `build.ps1` (PowerShell), где dot-source `setup_env.ps1` работает корректно.
- Ирина написала `build.ps1` сама, проверили — всё верно. Сборка прошла успешно.

## Где DeepSeek помог сильнее всего
1. Диагностика warning C4530 и решение через `/EHsc`.
2. Объяснение проблемы `.bat` + `setup_env.ps1` (переменные умирают в дочернем процессе).
3. Чёткая структура `build.ps1`, которую Ирина реализовала сама.

## Где DeepSeek ошибался / галлюцинировал
- Сборка C-версии с флагом `/std:c++latest` — бессмысленный флаг для C, хотя и безвредный. Ирина сама заметила.

## Что установлено / создано
- `build.ps1` — скрипт чистой сборки обеих версий
- Первый коммит в репозитории
- Настроен remote и git config

### 6. L2 — сортировка (пузырёк vs встроенная)

- Ирина уточнила постановку: ручная сортировка + встроенная для сравнения + замер времени.
- Написала обе версии сама: `sort_l2.c` и `sort_l2.cpp`.
- Первая итерация — ошибка: VLA `int arr1[n]` не поддерживается MSVC. Исправлено на `malloc`.
- C++: warning C4267 (`size_t` → `int`). Исправлено на `size_t`.
- Добавлена проверка `malloc` на NULL — убрала warnings C6011/C6387 от `/analyze`.
- На 7 элементах время одинаковое (0.000 мс) — агент объяснил: оба укладываются в точность таймера.
- Ирина увеличила массив до 10 000 элементов — разница стала видна.
- C: warning C4244 (`time_t` → `unsigned int`). Исправлено явным приведением: `(unsigned int)time(NULL)`.
- C++: генерация через `std::mt19937` + `std::uniform_int_distribution` — идиоматический подход.
- Обе версии собраны чисто (0 warnings). Результат: пузырёк ~400-500 мс, встроенная ~0-1 мс.

## Где DeepSeek помог сильнее всего
1. Диагностика warning C4530 и решение через `/EHsc`.
2. Объяснение проблемы `.bat` + `setup_env.ps1` (переменные умирают в дочернем процессе).
3. Чёткая структура `build.ps1`, которую Ирина реализовала сама.
4. Объяснение, почему время одинаковое на 7 элементах — точность таймера.
5. Диагностика VLA (C) — не поддерживается MSVC, замена на `malloc`.

## Где DeepSeek ошибался / галлюцинировал
- Сборка C-версии с флагом `/std:c++latest` — бессмысленный флаг для C, хотя и безвредный. Ирина сама заметила.
- Путаница с `<iomanip>` — агент предположил опечатку, но заголовок корректен.

## Что установлено / создано
- `build.ps1` — скрипт чистой сборки обеих версий
- Первый коммит в репозитории
- Настроен remote и git config
- `sort_l2.c` / `sort_l2.cpp` — L2 сортировки (пузырёк + встроенная + замеры)

## Анти-подхалимажные приёмы
- Ирина самостоятельно заметила лишний флаг `/std:c++latest` в C-сборке — пример критического отношения к выводу ИИ.

