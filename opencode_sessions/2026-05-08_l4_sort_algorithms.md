# Конспект: 2026-05-08 — L4 алгоритмы сортировки

## Контекст
L1 (линейный поиск), L2 (сортировка пузырьком vs std::sort), L3 (бинарный поиск) закрыты. L4 — ручная реализация трёх алгоритмов сортировки с замерами времени и сравнением с теорией.

## Ключевые шаги диалога

### 1. Постановка L4
- Ирина спросила, что дальше.
- Агент описал L4: merge sort, quick sort, heap sort руками, замеры на 1000/10000/100000, таблица O(N) vs реальное время.

### 2. C-версия (sort_l4.c)
- Ирина написала код, попросила рецензию.
- Агент нашёл: `srand` вместо `srand`, флаг `isHeap` в `measureTime`, утечка при ошибке `malloc`.
- Ирина поправила: исправила `srand`, добавила `heapSortWrapper`, освобождение памяти при ошибке.
- Код принят.

### 3. C++-версия (sort_l4.cpp)
- Ирина написала C++-код.
- Агент нашёл: `copyArray` лишняя (вектор сам копируется через `=`), `steady_clock` лучше заменить на `high_resolution_clock`.
- Ирина поправила, добавила строки в `build.ps1`.
- Код принят.

### 4. Git
- clang-format прогнан.
- Коммит готовится с сообщением: «Задача 7.1 L4: сортировки слиянием, быстрая, пирамидальная (C и C++)»

## Где DeepSeek помог сильнее всего
- Рецензия кода: нашёл нестандартный `srand`, предложил унификацию сигнатур через wrapper.

## Где галлюцинировал
- Не было.

## Анти-подхалимажные приёмы
- Не применялись.
