# Конспект: 2026-05-08 — L3 бинарный поиск

## Контекст
L1 (линейный поиск) и L2 (сортировка) завершены. Переход к L3 — бинарный поиск по отсортированной коллекции. Параллель C/C++ обязательна.

## Ключевые шаги диалога

### 1. Уточнение постановки L3
- Ирина уточнила: нужно ли писать и линейный, и бинарный поиск?
- Агент: нет, только бинарный; линейный — только для вывода числа сравнений (формулой или коротким циклом).
- Суть L3: показать O(log N) vs O(N) на конкретных цифрах.

### 2. Написание кода
- C-версия (`search_l3.c`): `malloc`, `qsort`, бинарный поиск вручную, подсчёт сравнений для обоих.
- C++-версия (`search_l3.cpp`): `std::vector`, `std::sort`, `std::mt19937`, бинарный поиск вручную, `std::cout`.
- Разница в подходах: C — ручное управление памятью, `rand()`; C++ — RAII, `<random>`, идиоматический вывод.

### 3. Коммит в git
- clang-format прогнан, сборка с `/W4 /analyze` без ошибок.
- Добавлены строки в `build.ps1` для L2 и L3.
- Добавлен § 11 в AGENTS.md (dialog_logs).
- При push возникли расхождения веток (удалённая ушла вперёд).
- Решено через `git stash` → `git pull --rebase` → `git stash pop` → `git push`.
- Ирина освоила `git stash`, `git pull --rebase`, `git stash pop`.

### Где DeepSeek помог сильнее всего
- Диагностика ошибок git (detach HEAD → rebase --abort; index contains uncommitted changes → stash).
- Предложил структуру коммита в стиле предыдущих.

### Где галлюцинировал
- Не было галлюцинаций в этой сессии.

### Анти-подхалимажные приёмы
- Не применялись — сессия была технической (компиляция, git).

